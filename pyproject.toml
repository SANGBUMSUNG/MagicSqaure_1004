[project]
name = "magic-square-1004"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
web = ["flask>=3.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
